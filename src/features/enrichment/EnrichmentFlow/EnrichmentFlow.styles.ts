/**
 * The header is the Maintainer's furniture (`features/maintainer.styles.ts`);
 * the flow has nothing of its own to add to it.
 */
export { HeaderRow, Heading, Lede } from '../../maintainer.styles';
