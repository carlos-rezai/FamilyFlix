/**
 * Motion tokens — translated 1:1 from `docs/handoff/tokens.css`
 * (`--dur-*`, `--ease-out`). No React, no logic — pure values.
 */
export const motion = {
  durFast: '120ms',
  durBase: '180ms',
  durSlow: '280ms',
  easeOut: 'cubic-bezier(0.2, 0.7, 0.3, 1)',
} as const;
